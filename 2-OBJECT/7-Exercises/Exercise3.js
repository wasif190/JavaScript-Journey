let student = {
    name: "David Rayy",
    sclass: "VI",
    rollno: 12
};
  
// Get the keys of the object
let keysArray = Object.keys(student);
  
// Get the length of the keys array
let objectLength = keysArray.length;
  
// Output the length of the object
console.log("Length of the object:", objectLength);
  