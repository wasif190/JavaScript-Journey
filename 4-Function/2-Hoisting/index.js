/*
Hoisting is a JavaScript Behavior where variable and function declarations (name for variable or functions) 
are moved to the top of their containing scope during the compilation phase
*/

add(2,5)
function add(a,b) {
    return a + b;
}
